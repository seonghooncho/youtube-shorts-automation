# Core dependencies
python-dotenv==1.0.1
requests==2.32.3
boto3==1.34.162

# Google APIs (YouTube)
google-api-python-client==2.137.0
google-auth==2.35.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0

# Optional platform uploaders are intentionally not installed by default.
# Enable and pin them separately only when Instagram/TikTok upload is actively used.

# Slack
slack_sdk==3.33.4
